# CHI — DIAGRAMS & VISUAL REFERENCES
# ════════════════════════════════════════════════════════════════════
# Protocol: AMBA CHI | Document: 02 of 08
# ════════════════════════════════════════════════════════════════════

---

## DIAGRAM 1: CHI System Topology

```
┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
│  RN-F   │  │  RN-F   │  │  RN-I   │  │  RN-D   │
│ (CPU 0) │  │ (CPU 1) │  │ (DMA)   │  │ (SMMU)  │
└────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘
     │             │             │             │
     │   TXREQ/TXDAT/TXRSP      │             │
     │   RXSNP/RXDAT/RXRSP      │             │
     │             │             │             │
┌────┴─────────────┴─────────────┴─────────────┴────┐
│              CHI INTERCONNECT (MESH/RING)           │
│                                                     │
│  ┌────────┐  ┌────────┐  ┌────────┐               │
│  │ HN-F   │  │ HN-F   │  │ HN-I   │               │
│  │(Snoop  │  │(Snoop  │  │(Device │               │
│  │Filter) │  │Filter) │  │ region)│               │
│  └────────┘  └────────┘  └────────┘               │
└──────────┬──────────────────┬──────────────────────┘
           │                  │
      ┌────┴────┐       ┌────┴────┐
      │  SN-F   │       │  SN-F   │
      │(DDR MC) │       │(DDR MC) │
      └─────────┘       └─────────┘
```

---

## DIAGRAM 2: CHI Channels Per Node

```
                  RN-F (CPU Core)
         ┌──────────────────────────────┐
         │                              │
    TX   │  TXREQ ──────────────────►   │  → To Interconnect
         │  TXRSP ──────────────────►   │
         │  TXDAT ──────────────────►   │
         │                              │
    RX   │  RXRSP ◄────────────────    │  ← From Interconnect
         │  RXDAT ◄────────────────    │
         │  RXSNP ◄────────────────    │
         │                              │
         └──────────────────────────────┘
         
Each channel: FLITV + FLIT[width] + LCRDV (credit return)
```

---

## DIAGRAM 3: ReadShared Transaction (Cache Miss, Clean Line)

```
   RN-F (CPU)          HN-F (Home)          SN-F (Memory)
       │                    │                    │
   ①   │───ReadShared──────►│                    │
       │   (TXREQ)          │                    │
       │                    │                    │
       │                    │──ReadNoSnp────────►│
       │                    │   (to memory)      │
       │                    │                    │
       │                    │◄──CompData(UC)─────│
   ②   │◄──CompData(UC)────│   (RXDAT)          │
       │   (RXDAT)          │                    │
       │                    │                    │
   ③   │───CompAck─────────►│                    │
       │   (TXRSP)          │                    │
       │                    │                    │
       
Result: RN-F has line in UC (Unique Clean) state
```

---

## DIAGRAM 4: ReadUnique with Snoop (Dirty Hit in Another Cache)

```
   RN-F₀ (Req)         HN-F (Home)          RN-F₁ (has UD)
       │                    │                    │
   ①   │───ReadUnique──────►│                    │
       │                    │                    │
       │                    │───SnpUnique───────►│  ②
       │                    │                    │
       │                    │◄──SnpRespData(I)──│  ③
       │                    │   (dirty data)     │
       │                    │                    │
   ④   │◄──CompData(UD)────│                    │
       │                    │                    │
   ⑤   │───CompAck─────────►│                    │
       │                    │                    │

Result: RN-F₀ has line in UD, RN-F₁ invalidated
HN-F updates snoop filter: line owned by RN-F₀
```

---

## DIAGRAM 5: Direct Cache Transfer (DCT)

```
   RN-F₀ (Req)         HN-F (Home)          RN-F₁ (has UD)
       │                    │                    │
   ①   │───ReadShared──────►│                    │
       │                    │                    │
       │                    │───SnpSharedFwd────►│  ②
       │                    │   (FwdNID=RN-F₀)   │
       │                    │                    │
   ③   │◄──────────────CompData(SC)─────────────│  DIRECT!
       │   (from RN-F₁ directly, bypass HN)     │
       │                    │                    │
       │                    │◄──SnpResp(SD)──────│  ④
       │                    │                    │
   ⑤   │───CompAck─────────►│                    │
       │                    │                    │

Result: Low latency — data goes peer-to-peer
RN-F₁ keeps line as SD (Shared Dirty)
RN-F₀ gets line as SC (Shared Clean)
```

---

## DIAGRAM 6: WriteBack Transaction

```
   RN-F (CPU)          HN-F (Home)          SN-F (Memory)
       │                    │                    │
   ①   │──WriteBackFull────►│                    │
       │  (evicting UD)     │                    │
       │                    │                    │
   ②   │◄──CompDBIDResp────│                    │
       │  (buffer allocated) │                    │
       │                    │                    │
   ③   │──CBWrData(64B)───►│                    │
       │  (TXDAT)           │                    │
       │                    │──WriteNoSnp───────►│  ④
       │                    │  (to memory)       │
       │                    │                    │

Result: Dirty line written back to memory
HN-F removes RN-F from snoop filter for this line
```

---

## DIAGRAM 7: Credit-Based Flow Control

```
Time ──►

Receiver           Transmitter
    │                   │
    │──LCRDV=1─────────►│  Credit +1 (total=1)
    │──LCRDV=1─────────►│  Credit +1 (total=2)
    │──LCRDV=1─────────►│  Credit +1 (total=3)
    │                   │
    │◄──FLITV=1,FLIT────│  Send flit (credit=2)
    │◄──FLITV=1,FLIT────│  Send flit (credit=1)
    │◄──FLITV=1,FLIT────│  Send flit (credit=0) MUST STOP
    │                   │
    │  [processing...]  │  Cannot send (no credits)
    │                   │
    │──LCRDV=1─────────►│  Credit +1 (total=1)
    │◄──FLITV=1,FLIT────│  Send flit (credit=0)
    │                   │
```

---

## DIAGRAM 8: Coherence State Transitions

```
         MakeUnique/
         ReadUnique          WriteBack/
    ┌───────────────── UD ◄─────── UC ◄──── ReadUnique (miss)
    │                  │           │
    │     Snoop        │  Snoop    │  Snoop
    │    (SnpShared)   ▼  (Clean)  ▼  (SnpUnique)
    │                  SD ────────► SC
    │                  │            │
    │    SnpUnique     │ SnpUnique  │
    │                  ▼            ▼
    └─────────────────► I ◄─────────┘
                     (Invalid)
                     
Legend:
  UC = Unique Clean     UD = Unique Dirty
  SC = Shared Clean     SD = Shared Dirty
  I  = Invalid
```

---

END OF DOCUMENT 02 — DIAGRAMS
